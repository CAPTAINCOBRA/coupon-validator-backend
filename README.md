# coupon-validator-backend
Backend server for coupon validator web app. Built using Express.


Run - "npm install" in the root folder to install the dependencies 
Then run "npm start" to run the project


